package com.the_setox.domain.app

import com.the_setox.domain.NotesRepository

/**
 * A class used for generic creation and common methods specifically for Notes UseCase.
 */
open class NotesUseCase(val repository: NotesRepository) {
    //TODO Add generic implementation or methods for NoteUseCase here.
}